{
  "name": "webart",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 192.168.1.3 --port 58707",
    "check": "npm run test && npm run build",
    "test": "vitest run --config vitest.config.js"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
